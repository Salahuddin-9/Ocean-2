# Turtle Backend Import Graph — Analysis Report

Generated by `scripts/analyze-turtle-imports.mjs` (full JSON: `scripts/turtle-import-graph.json`).

## Headline finding

**There are 0 dead/orphan turtle modules in the current codebase.** The premise of
"~166 turtle backend modules not imported by server.ts / chatServer.ts /
turtleFeatureRegistry.ts" is **outdated** — the wiring/archiving pass was already
completed in a prior session. Evidence:

- `src/` contains **163** `turtle*.ts` modules; every one of them is imported by at
  least one file in the repo.
- **148** are imported by `src/turtleFeatureRegistry.ts`, and all 148 `register*`
  functions are actually invoked inside `registerOceanFeatures(app)` (no imported-but-
  unused registrations).
- The remaining **15** are imported by `server.ts`, `chatServer.ts`, components, or
  shared helper modules (not dead — they are engines/helpers, not route modules).
- Already archived: `src/archive/dead-turtle/` (14 superseded backends + README) and
  `docs/specs/` (4 ATLAS-RANK design specs) exist from the prior cleanup.

## Summary counts

| Category | Count |
|---|---|
| Total `src/turtle*.ts` modules | 163 |
| Imported by `turtleFeatureRegistry.ts` (wired features) | 148 |
| Imported elsewhere (server.ts / chatServer.ts / components / helpers) | 15 |
| **Dead (0 imports)** | **0** |
| Registry `register*` imports that are never called | 0 |

## Modules NOT wired via the registry (all alive via other importers)

| Module | Importer(s) | Role |
|---|---|---|
| turtleAIBengaliModerationEngine.ts | server.ts | engine registered directly in server.ts |
| turtleAICaptionEngine.ts | server.ts | registered directly in server.ts |
| turtleAIModerationAssistant.ts | server.ts | registered directly in server.ts |
| turtleAIVehicleAnalysisEngine.ts | server.ts | registered directly in server.ts |
| turtleChatAiHelper.ts | chatServer.ts, server.ts | chat copilot used by WS + HTTP |
| turtleCoinTransfer.ts | 7 backends | shared wallet helpers (transferCoins, pushNotification, haversineKm) |
| turtleCommunityBackend.ts | server.ts + 30 backends | shared community engine (coins, events) |
| turtleEmergencyPools.ts | 12 backends + SOSEmergencyButton | shared rate-limit/safety engine |
| turtleEmergencyPoolsBackend.ts | server.ts | registered directly in server.ts |
| turtleFeatureRegistry.ts | server.ts | the registry itself |
| turtleJsonStore.ts | 10 backends | shared JSON store helper |
| turtleOfflineP2P.ts | OfflineChatView.tsx, OfflineMeshFab.tsx | client P2P engine |
| turtleRankingEngine.ts | App.tsx, hybridRanker.ts | feed ranking engine |
| turtleReelsBackend.ts | server.ts, App.tsx | reels engine + client caption generator |
| turtleSecurityTelegramOTPService.ts | server.ts | registered directly in server.ts |

## Prior archival (already present, verified)

- `src/archive/dead-turtle/` — 14 superseded/duplicate backends (turtleAuthFlow,
  turtleLogic, turtleMessagingBackend, turtleNotificationSystem, turtleFriendsSystem,
  turtleReactionSystem, turtleChannelsBackend, turtleModerationSystem,
  turtleRandomChatBackend, turtleProfileMetrics, turtleSecurityPrivacyBackend,
  turtleUserSettingsBackend, turtleAICaptionFlow, turtleFeedPostLogic) + README.
  Excluded from `tsc` via tsconfig `exclude: ["src/archive/**"]`.
- `docs/specs/` — `01-architecture.md`, `02-ranking.md`, `03-platform.md`,
  `04-meta-tiktok-production-gap-analysis.md` (ATLAS-RANK design specs).

## Conclusion

No further wiring or archiving is required: every feature backend (109–260) has a
live registration path, and the only duplicate/spec modules were already archived.
